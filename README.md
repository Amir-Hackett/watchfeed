# watchfeed — published site

The built calendar served at
[amir-hackett.github.io/watchfeed](https://amir-hackett.github.io/watchfeed/).

This repository holds **only generated output**. It is rebuilt and force-pushed
by the `watchfeed` workflow in the private source repository; edits here are
overwritten on the next build.
